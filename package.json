{
  "dependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "d": "concurrently \"nodemon server.js\" \"npm start --prefix client\" "
  }
}

